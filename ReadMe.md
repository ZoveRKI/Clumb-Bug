# Clumb Bug

## Before Action
make sure you had install:

    pip install questionary beautifulsoup4 requests

maybe you need `types-beautifulsoup4` to resolve warning:

    pip install types-beautifulsoup4
